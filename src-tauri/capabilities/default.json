{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Joi main window. App commands are exposed via the invoke handler and need no extra permission. Window-control permissions back the custom titlebar (decorations:false): drag-to-move, minimize, maximize toggle, double-click maximize, and close.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-internal-toggle-maximize",
    "core:window:allow-close"
  ]
}
