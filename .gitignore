# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Node / frontend
node_modules
dist
.vite
*.tsbuildinfo

# Editor / OS
.DS_Store
*.swp

# Local env
.env
.env.local

# Real app config lives at ~/.config/joi/joi.yaml (outside the repo). If one is ever placed in the
# repo's config/ dir, keep it out of source control — only the example is tracked.
/config/*
!/config/joi.example.yaml

# Tauri build-generated schemas
/src-tauri/gen/
