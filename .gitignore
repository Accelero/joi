# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Node / frontend
node_modules
dist
.vite
*.tsbuildinfo

# Editor / OS
.DS_Store
*.swp

# Local env
.env
.env.local

# Real app config lives at ~/.joi/config.json (outside the repo). If one is ever placed in the
# repo's config/ dir, keep it out of source control — only the example is tracked.
/config/*
!/config/joi.example.json

# Claude Code transient worktrees
/.claude/worktrees/
